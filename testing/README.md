# Testing

- Write the plan before starting a test.
- Save results even when the test fails.
- Put unexpected problems in `failures/` so the team can learn from them.
- Never test a real inflation device on a person during normal development.

