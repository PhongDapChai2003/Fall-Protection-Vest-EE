# Python tools

- `data_collection/`: scripts that record sensor data
- `analysis/`: scripts that clean, compare, or plot data
- `notebooks/`: experiments and notes

Create a virtual environment, install `requirements.txt`, and keep personal information out of saved datasets.

